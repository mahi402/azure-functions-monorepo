{
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "mongodb": "^4.17.0",
    "mongoose": "^6.11.3",
    "repos-model": "^0.0.45"
   
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "@types/node": "^22.9.0",
    "tsup": "latest",
    "typescript": "latest",
    "repos-model": "^0.0.45"
  
  }
}
